{

  "name": "demoseleniumproject",

  "version": "1.0.0",

  "description": "",

  "main": "index.js",

  "scripts": {

    "test": "jest --silent",
    "build": "cross-env NODE_ENV=development ASSETS_BUILD_TYPE=prepub ASSETS_PROJECT_PATH=${npm_package_name/test-} ASSETS_BUILD_VERSION=$npm_package_version  env-cmd -e development webpack --mode=production"
  },

  "author": "",

  "license": "ISC",

  "dependencies": {

    "selenium-webdriver": "^4.11.1",
    
    "jest": "^29.7.0",
    
    "chromedriver": "^127.0.0"

  }

}

